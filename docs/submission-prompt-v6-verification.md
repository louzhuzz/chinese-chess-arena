# v6 提交提示词修复与复测

日期：2026-09-10。版本：`xiangqi-move-v6-submit-once`。

## 改动

- 当前局面仅发送完整 pieces，删除重复 FEN 和字符棋盘。棋谱保留逐步 FEN。
- 明确候选选定并确认在 legal_moves 中就提交，不重新证明最优或重建历史。
- 请求新增 output_token_limit，首答及纠错分别使用其实际配置的上限。
- 首答保留所选思考强度；没有加入评分、搜索或推荐走法。

## 有限真实复测

同一历史局面、DeepSeek Flash、low、16,384 上限；每个局面一次新采样。对照为此前记录的失败调用，不是随机重复 A/B，不能推断长期成功率或棋力。

|半回合|原输出 token|新输出 token|新耗时（秒）|结果|
|---|---:|---:|---:|---|
|8|16383|6096|34.44|合法提交|
|11|16384|13626|71.36|合法提交|
|13|16384|16384|78.72|仍为空答，length|
|19|16383|16384|80.09|仍为空答，length|

四条完整记录共输出 52,490 token，缓存命中 1,536，未命中 3,319。按官方 Flash 高峰价格估算这些完整记录费用约 $0.06399。执行曾中断，可能存在未保存的调用用量，因此该值不是完整账单。价格来源：[DeepSeek Pricing](https://api-docs.deepseek.com/quick_start/pricing/)。

**结论：2/4 恢复正常，2/4 仍耗尽预算。只能认定输入契约和提示词修改完成，不能认定 16K 空答已根治。** 保留原来的关闭思考纠错兜底；没有以增加上限或替模型选棋掩盖失败。

原始复测记录：`run/submission-prompt-live-results.json`。复测脚本需要显式 `--live`，会产生 API 费用；默认 pytest 不调用真实模型。

## 测试隔离事故与修复

本次执行 API 测试时，夹具先导入 main，后重载临时数据库配置。main 模块初始化使用了之前缓存的正式数据库路径，触发 Database.init 的中断标记，误将 fcde1fac934441a787db62f996a2c013 在第 36 个半回合标为 interrupted/process_restart。不是棋规终局，也不是模型技术负。

已调整夹具顺序：先重载 config/db，再导入 main。新增子进程回归，用临时 sentinel 数据库模拟运行中棋局；修复前能重现中断，修复后状态保持 running。没有自动续下或篡改结果。已保存的 36 手数据另存 run/fcde1fac-interrupted-preserved.json。

验证：8 项提示词/输入/纠错针对性检查通过；另 1 项隔离回归通过。服务重新启动后 /api/health 返回 200。
